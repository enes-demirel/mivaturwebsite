import Link from "next/link";import { saveDestinationAction } from "@/app/admin/(protected)/destinasyonlar/actions";import { DestinationForm } from "@/components/admin/destinations/destination-form";
export default async function NewDestinationPage({searchParams}:{searchParams:Promise<{error?:string}>}){const{error}=await searchParams;return <div className="mx-auto max-w-5xl"><Link href="/admin/destinasyonlar" className="text-sm font-bold text-brand">← Destinasyon listesine dön</Link><h1 className="mt-5 text-3xl font-extrabold">Yeni Destinasyon</h1>{error&&<p role="alert" className="mt-5 rounded-md border border-brand/20 bg-brand/5 p-3 text-sm font-semibold text-brand">{error==="slug"?"Bu slug başka bir destinasyonda kullanılıyor.":"Form alanlarını kontrol edin."}</p>}<div className="mt-8"><DestinationForm action={saveDestinationAction.bind(null,null)}/></div></div>}
