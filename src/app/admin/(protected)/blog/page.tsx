import Link from "next/link";import { listAdminBlogs } from "@/lib/db/repositories/admin-content";
export default async function AdminBlogPage(){const posts=await listAdminBlogs();return <div className="mx-auto max-w-6xl"><header className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-extrabold tracking-[.16em] text-brand uppercase">İçerik Yönetimi</p><h1 className="mt-2 text-3xl font-extrabold">Blog</h1></div><Link href="/admin/blog/yeni" className="inline-flex min-h-11 items-center rounded-md bg-brand px-5 text-sm font-bold text-white">Yeni Yazı</Link></header><div className="mt-8 overflow-x-auto rounded-lg border border-border bg-surface shadow-card"><table className="w-full min-w-[680px] text-left text-sm"><thead className="border-b border-border bg-background text-muted"><tr><th className="p-4">Başlık</th><th className="p-4">Durum</th><th className="p-4">Yayın</th><th className="p-4 text-right">İşlem</th></tr></thead><tbody>{posts.map(post=><tr key={post.id} className="border-b border-border last:border-0"><td className="p-4 font-bold">{post.title}</td><td className="p-4">{post.status}</td><td className="p-4">{post.published_at?new Intl.DateTimeFormat("tr-TR",{dateStyle:"medium"}).format(new Date(post.published_at)):"—"}</td><td className="p-4 text-right"><Link href={`/admin/blog/${post.id}`} className="font-bold text-brand">Düzenle</Link></td></tr>)}</tbody></table>{posts.length===0&&<p className="p-8 text-center text-muted">Henüz blog yazısı yok.</p>}</div></div>}
