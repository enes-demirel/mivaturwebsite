import Link from "next/link";import { saveBlogAction } from "@/app/admin/(protected)/blog/actions";import { BlogForm } from "@/components/admin/blog/blog-form";
export default async function NewBlogPage({searchParams}:{searchParams:Promise<{error?:string}>}){const{error}=await searchParams;return <div className="mx-auto max-w-5xl"><Link href="/admin/blog" className="text-sm font-bold text-brand">← Blog listesine dön</Link><h1 className="mt-5 text-3xl font-extrabold">Yeni Blog Yazısı</h1>{error&&<p role="alert" className="mt-5 rounded-md border border-brand/20 bg-brand/5 p-3 text-sm font-semibold text-brand">{error==="slug"?"Bu slug başka bir yazıda kullanılıyor.":"Zorunlu alanları kontrol edin."}</p>}<div className="mt-8"><BlogForm action={saveBlogAction.bind(null,null)}/></div></div>}
