import type { Metadata } from "next";import { Container } from "@/components/ui/container";import { legalConfig } from "@/data/legal-config";import { createSiteWhatsAppUrl,siteConfig } from "@/data/site-config";
export const metadata:Metadata={title:"İletişim",description:"Mivatur telefon, e-posta ve ofis iletişim bilgileri.",alternates:{canonical:"/iletisim"}};
export default function ContactPage(){return <Container className="py-12 sm:py-16 lg:py-24"><header className="max-w-3xl"><p className="text-sm font-extrabold tracking-[.14em] text-brand uppercase">İletişim</p><h1 className="mt-3 text-4xl leading-tight font-extrabold tracking-tight sm:text-5xl">Yolculuğunuzu birlikte planlayalım</h1><p className="mt-5 text-lg leading-8 text-muted">Tur programları ve rezervasyon talepleriniz için ekibimize telefon, WhatsApp veya e-posta üzerinden ulaşabilirsiniz.</p></header><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-4"><ContactCard title="Telefon" value={legalConfig.phone} href={`tel:${legalConfig.phoneHref}`}/><ContactCard title="WhatsApp" value={siteConfig.whatsappDisplay} href={createSiteWhatsAppUrl()} external/><ContactCard title="E-posta" value={legalConfig.email} href={`mailto:${legalConfig.email}`}/><ContactCard title="Ofis" value={legalConfig.address}/></div></Container>}
function ContactCard({title,value,href,external=false}:{title:string;value:string;href?:string;external?:boolean}){return <section className="rounded-lg border border-border bg-surface p-6 shadow-card"><h2 className="text-sm font-extrabold tracking-[.12em] text-brand uppercase">{title}</h2>{href?<a href={href} target={external?"_blank":undefined} rel={external?"noopener noreferrer":undefined} className="mt-3 block break-words font-bold leading-7 text-text outline-none hover:text-brand focus-visible:ring-3 focus-visible:ring-brand/20">{value}</a>:<p className="mt-3 font-semibold leading-7 text-text">{value}</p>}</section>}
