export function ContentSectionCard({ id, title, summary, defaultOpen = false, children }: { id: string; title: string; summary: string; defaultOpen?: boolean; children: React.ReactNode }) {
  return <details open={defaultOpen} className="group overflow-hidden rounded-lg border border-border bg-surface shadow-card"><summary className="flex min-h-16 cursor-pointer list-none items-center justify-between gap-4 px-5 py-4 focus-visible:outline-2 focus-visible:outline-offset-[-2px] focus-visible:outline-brand sm:px-6"><span><span id={id} className="block font-extrabold text-text">{title}</span><span className="mt-0.5 block text-sm text-muted">{summary}</span></span><span aria-hidden="true" className="text-xl text-brand transition-transform group-open:rotate-45">+</span></summary><div className="border-t border-border p-5 sm:p-6">{children}</div></details>;
}
