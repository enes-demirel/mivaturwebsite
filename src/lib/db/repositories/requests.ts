import "server-only";
import { run } from "@/lib/db/query";
export async function createReservationRequest(input:{tourId?:string;departureId?:string;fullName:string;phone:string;email?:string;participantCount:number;message?:string}) { const now=new Date().toISOString(); return run("INSERT INTO reservation_requests (id,tour_id,departure_id,full_name,phone,email,participant_count,message,status,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,'new',?,?)",[crypto.randomUUID(),input.tourId??null,input.departureId??null,input.fullName,input.phone,input.email??null,input.participantCount,input.message??null,now,now]); }
export async function createCustomTourRequest(input:{fullName:string;phone:string;email?:string;destination?:string;startDate?:string;endDate?:string;participantCount:number;accommodationPreference?:"hotel"|"apartment"|"no-preference";transportationPreference?:string;budget?:string;notes?:string}) { const now=new Date().toISOString(); return run("INSERT INTO custom_tour_requests (id,full_name,phone,email,destination,start_date,end_date,participant_count,accommodation_preference,transportation_preference,budget,notes,status,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,'new',?,?)",[crypto.randomUUID(),input.fullName,input.phone,input.email??null,input.destination??null,input.startDate??null,input.endDate??null,input.participantCount,input.accommodationPreference??null,input.transportationPreference??null,input.budget??null,input.notes??null,now,now]); }
